<% content_for(:title, "Edit Station | BusSystem") %>

<h1 class="jumbotron">Edit <%= @station.name %></h1>

<%= render('layouts/errors', :object => @station) %>
<%= render 'form' %>

<%= link_to "Back to All Stations", stations_path, :class => "btn btn-primary" %>
